name: Build and publish

on:
  push:
    branches:
      - master
    tags:
      - v**
  pull_request:
    branches:
      - master

jobs:
  build-and-publish:
    uses: ./master-build-and-publish.yml@v1
    secrets:
      npm_auth_token: ${{ secrets.NPM_AUTH_TOKEN }}