name: Build and publish

on:
  push:
    branches:
      - master
    tags:
      - v**
  pull_request:
    branches:
      - master

jobs:
  build-and-publish:
    uses: ./.github/workflows/master-build-and-publish.yml
    with:
      package-manager-version: .github/labeler.yml
    secrets:
      npm_auth_token: ${{ secrets.NPM_AUTH_TOKEN }}